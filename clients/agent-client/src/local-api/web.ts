export function renderConsoleHtml() {
  return `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta http-equiv="Cache-Control" content="no-store" />
  <title>FreeChat Agent Client</title>
  <style>
    :root{--bg:#eef3ff;--panel:#ffffffd9;--panel-solid:#fff;--text:#0f172a;--muted:#64748b;--line:#dbe4f0;--brand:#2563eb;--brand-2:#7c3aed;--brand-soft:#eaf1ff;--ok:#059669;--bad:#dc2626;--warn:#d97706;--shadow:0 18px 45px #1e293b14;--shadow-soft:0 8px 24px #1e293b0d}*{box-sizing:border-box}body{margin:0;min-height:100vh;font-family:Inter,-apple-system,BlinkMacSystemFont,'Segoe UI','PingFang SC','Microsoft YaHei',sans-serif;color:var(--text);background:radial-gradient(circle at 10% 0%,#dbeafe 0,#eef3ff 34%,transparent 35%),radial-gradient(circle at 90% 10%,#ede9fe 0,#f5f3ff 28%,transparent 29%),linear-gradient(180deg,#f8fbff 0%,#eef3ff 100%)}body:before{content:'';position:fixed;inset:0;background-image:linear-gradient(#ffffff80 1px,transparent 1px),linear-gradient(90deg,#ffffff80 1px,transparent 1px);background-size:42px 42px;mask-image:linear-gradient(180deg,#0008,transparent 70%);pointer-events:none}header{padding:18px 24px;background:linear-gradient(135deg,#0f172a 0%,#1e3a8a 55%,#5b21b6 100%);color:white;display:flex;justify-content:space-between;gap:16px;align-items:center;flex-wrap:wrap;position:sticky;top:0;z-index:10;box-shadow:0 14px 35px #0f172a33}header strong{font-size:18px;letter-spacing:.2px}.brand-subtitle{margin-top:4px;color:#cbd5e1;font-size:13px}main{max-width:1160px;margin:0 auto;padding:22px;display:grid;gap:18px;position:relative}h2{font-size:18px;margin:0 0 12px;letter-spacing:.2px}h3{font-size:16px;margin:0 0 8px}.card{background:var(--panel);border:1px solid #ffffffb8;border-radius:22px;padding:18px;box-shadow:var(--shadow-soft);backdrop-filter:blur(14px)}.card:hover{box-shadow:var(--shadow)}.row{display:flex;gap:10px;flex-wrap:wrap;align-items:center}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px}.tabs{display:flex;gap:10px;overflow:auto;padding:4px 2px 8px;scrollbar-width:none}.tabs::-webkit-scrollbar{display:none}.tab{background:#ffffffb8;color:#334155;border:1px solid #e2e8f0;border-radius:999px;padding:10px 16px;min-height:40px;box-shadow:0 6px 16px #1e293b0a}.tab.active{background:linear-gradient(135deg,var(--brand),var(--brand-2));color:white;border-color:transparent;box-shadow:0 12px 28px #2563eb30}input,select,button{font:inherit;border-radius:13px;min-height:44px}input,select{flex:1;min-width:190px;border:1px solid var(--line);padding:11px 12px;background:#fff;color:var(--text);outline:none;box-shadow:inset 0 1px 0 #ffffff}input:focus,select:focus{border-color:#93c5fd;box-shadow:0 0 0 4px #bfdbfe66}button{border:0;padding:11px 15px;background:linear-gradient(135deg,var(--brand),#1d4ed8);color:white;cursor:pointer;white-space:nowrap;font-weight:600;box-shadow:0 10px 22px #2563eb26;transition:transform .15s ease,box-shadow .15s ease,opacity .15s ease}button:hover:not(:disabled){transform:translateY(-1px);box-shadow:0 14px 26px #2563eb32}button:disabled{opacity:.55;cursor:not-allowed;box-shadow:none}button.secondary{background:#475569;box-shadow:0 10px 20px #47556922}button.danger,.danger{background:linear-gradient(135deg,#ef4444,#dc2626)}.ok{color:var(--ok)}.bad{color:var(--bad)}.muted{color:var(--muted);font-size:13px;line-height:1.55}pre{background:#0b1020;color:#d1d5db;border-radius:16px;padding:14px;overflow:auto;max-height:380px;border:1px solid #1e293b}.agent{border:1px solid #e2e8f0;border-radius:18px;padding:15px;min-width:0;background:linear-gradient(180deg,#fff,#f8fafc);box-shadow:0 8px 20px #1e293b0a}.agent h3{display:flex;align-items:center;gap:8px}.agent h3:before{content:'';width:9px;height:9px;border-radius:999px;background:linear-gradient(135deg,#22c55e,#2563eb);box-shadow:0 0 0 4px #dcfce7}.pill{display:inline-flex;align-items:center;border-radius:999px;padding:4px 9px;background:var(--brand-soft);color:#1d4ed8;font-size:12px;font-weight:700;border:1px solid #dbeafe}.login-card{max-width:440px;margin:14vh auto 0;padding:24px}.hidden{display:none!important}.notice{margin-top:12px;padding:11px 12px;border-radius:14px;background:#f1f5f9;color:#334155;border:1px solid #e2e8f0}.notice.bad{background:#fef2f2;color:#b91c1c;border-color:#fecaca}.notice.ok{background:#ecfdf5;color:#047857;border-color:#bbf7d0}.setting-line{display:grid;grid-template-columns:160px 1fr;gap:10px;margin:9px 0;padding:8px 0;border-bottom:1px dashed #e2e8f0}.tab-panel{display:grid;gap:18px}.form-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px}.field{display:grid;gap:7px}.field span{font-size:13px;font-weight:700;color:#475569}.actions{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-top:14px}.status-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:10px}.status-grid p{background:#f8fafc;border:1px solid #e2e8f0;border-radius:14px;padding:10px 12px}.settings-summary{margin-top:12px}.edit-layout{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(280px,.85fr);gap:18px}.knowledge-list{display:grid;gap:10px}.knowledge-item{border:1px solid #e2e8f0;background:#f8fafc;border-radius:14px;padding:10px;overflow-wrap:anywhere}.textarea{min-height:110px;resize:vertical;border:1px solid var(--line);border-radius:13px;padding:11px 12px;background:#fff;color:var(--text);font:inherit}.wide{grid-column:1/-1}.settings-summary{margin-top:12px}.managed-layout{display:grid;grid-template-columns:minmax(240px,320px) minmax(0,1fr);gap:18px;min-width:0}.managed-layout>.card{min-width:0}.room-list{display:grid;gap:10px;min-width:0}.room-item{width:100%;display:block;text-align:left;background:#fff;color:var(--text);border:1px solid #e2e8f0;box-shadow:none;white-space:normal;overflow-wrap:anywhere}.room-item:hover{transform:none}.room-item.active{border-color:#93c5fd;background:#eff6ff}.message-list{display:grid;gap:10px;max-height:620px;overflow:auto;padding-right:4px;min-width:0}.msg{min-width:0;border:1px solid #e2e8f0;background:#f8fafc;border-radius:16px;padding:10px 12px;overflow:hidden}.msg.ai{background:#eef2ff;border-color:#c7d2fe}.msg.system{background:#f1f5f9}.msg-head{display:flex;justify-content:space-between;gap:8px;color:#64748b;font-size:12px;margin-bottom:5px;flex-wrap:wrap}.msg-content{white-space:pre-wrap;line-height:1.55;overflow-wrap:anywhere;word-break:break-word}#summary,#quickStatus,#accountInfo,#health,#settings{line-height:1.7}#summary p,#quickStatus p,#accountInfo p,#health p{margin:6px 0}.login-card h2:before{content:'🔐 ';font-size:20px}.card h2:after{content:'';display:block;width:42px;height:3px;border-radius:999px;background:linear-gradient(90deg,var(--brand),var(--brand-2));margin-top:8px}@media (max-width:640px){header{padding:15px 14px;border-radius:0 0 20px 20px}main{padding:14px}.card{padding:15px;border-radius:18px}.grid{grid-template-columns:1fr}.row{display:grid;grid-template-columns:1fr;align-items:stretch}input,select,button{width:100%;font-size:16px}.login-card{margin:8vh 0 0}pre{max-height:260px;font-size:12px}.agent .row{grid-template-columns:1fr 1fr}.tabs{position:sticky;top:72px;background:#eef3ffcc;backdrop-filter:blur(12px);z-index:9;padding:8px 0}.setting-line{grid-template-columns:1fr}.actions{display:grid;grid-template-columns:1fr}.status-grid{grid-template-columns:1fr}.managed-layout{grid-template-columns:1fr}.edit-layout{grid-template-columns:1fr}.message-list{max-height:520px}.brand-subtitle{font-size:12px}}
  </style>
</head>
<body>
<header><div><strong>FreeChat Agent Client</strong><div class="brand-subtitle">连接 FreeChat Server，自动接收 @Agent 请求并在本机回复</div></div></header>
<main>
  <section class="card login-card" id="login"><h2>管理员登录</h2><div class="row"><input id="password" type="password" placeholder="管理员密码" autocomplete="current-password"><button id="loginBtn" onclick="login()">登录</button></div><div id="loginMsg" class="notice hidden"></div><p class="muted">公网访问必须登录后才能查看客户端状态和管理 Agent。</p></section>
  <section id="app" class="hidden">
    <nav class="tabs" id="tabs"></nav>
    <section class="tab-panel" data-panel="account">
      <section class="card account-hero"><h2>当前账号</h2><div id="accountInfo"></div><div id="summary" class="status-grid"></div><div id="serverMsg" class="notice hidden"></div><div class="actions"><button class="secondary" onclick="logout()">退出客户端</button></div></section>
      <section class="card"><h2>登录账号</h2><p class="muted">填写一次 FreeChat 账号，之后登录本客户端会自动连接并显示这个账号的 Agent。</p><div class="form-grid"><label class="field"><span>服务地址</span><input id="serverUrl" placeholder="如 http://127.0.0.1:3001"></label><label class="field"><span>用户名</span><input id="serverUsername" placeholder="FreeChat 用户名"></label><label class="field"><span>密码</span><input id="serverPassword" type="password" placeholder="留空则不修改已保存密码"></label></div><div class="actions"><button onclick="saveAllConfig()">保存并连接</button><button class="secondary" onclick="testServerConn()">测试服务地址</button><button class="secondary" onclick="clearFreeChatAccount()">清除账号</button></div></section>
      <section class="card"><h2>接收请求</h2><div id="quickStatus" class="status-grid"></div><div class="actions"><button onclick="resumeReceiving()">恢复接收请求</button><button class="secondary" onclick="pauseReceiving()">暂停接收请求</button></div><p class="muted">当服务端有 @Agent 或任务分派时，本客户端会自动接收并处理。</p></section>
    </section>
    <section class="tab-panel hidden" data-panel="agents"><section class="card"><h2>Agent 管理</h2><div class="row"><button onclick="loadServerAgents()">刷新 Agent 列表</button><button class="secondary" type="button" onclick="switchTab('create')">去新建 Agent</button></div><div id="agentMsg" class="notice hidden"></div><p class="muted">列表只做浏览、接管、上架等快捷操作；复杂编辑请进入独立编辑页面。</p></section><section class="card"><h2>我的 Agent</h2><div id="serverAgents" class="grid"></div></section></section>
    <section class="tab-panel hidden" data-panel="create"><section class="card"><h2>新建 Agent</h2><p class="muted">创建后会默认由客户端执行，并自动接管到本客户端。Agent 本身不区分专家/助理；是否作为房间助理由房间配置决定。</p><div class="form-grid"><label class="field"><span>名称</span><input id="createAgentName" placeholder="例如 连载作家"></label><label class="field"><span>执行位置</span><select id="createAgentDeployment"><option value="client">客户端执行</option></select></label></div><div class="form-grid"><label class="field"><span>描述</span><input id="createAgentDesc" placeholder="这个 Agent 负责什么"></label><label class="field"><span>专长</span><input id="createAgentSpecialties" placeholder="逗号分隔，例如 剧情,分镜"></label></div><div class="actions"><button type="button" onclick="createAgentFromStandaloneForm()">创建并接管</button><button class="secondary" type="button" onclick="switchTab('agents')">返回 Agent 管理</button></div><div id="createAgentMsg" class="notice hidden"></div></section></section>
    <section class="tab-panel hidden" data-panel="agentEdit"><section class="card"><h2 id="agentEditTitle">编辑 Agent</h2><p class="muted">这里集中维护 Agent 的中心配置和本客户端运行信息；知识库归属客户端，服务端不保存知识库正文。</p><div id="agentEditMsg" class="notice hidden"></div></section><section class="edit-layout"><section class="card"><h2>中心配置</h2><div class="form-grid"><label class="field"><span>名称</span><input id="editAgentName" placeholder="Agent 名称"></label><label class="field"><span>执行位置</span><select id="editAgentDeployment"><option value="client">客户端执行</option></select></label><label class="field wide"><span>描述</span><input id="editAgentDesc" placeholder="这个 Agent 负责什么"></label><label class="field wide"><span>专长</span><input id="editAgentSpecialties" placeholder="逗号分隔，例如 剧情,分镜"></label></div><div class="actions"><button id="saveAgentBtn" type="button">保存 Agent</button><button id="bindAgentFromEditBtn" type="button" class="secondary">由本客户端接管执行</button><button class="secondary" type="button" onclick="switchTab('agents')">返回列表</button></div></section><section class="card"><h2>客户端知识库</h2><p class="muted">知识库文件保存在本 Agent 的客户端本地目录，运行时会写入 .freechat/KNOWLEDGE.md 供 Agent 按需读取。</p><div id="agentKnowledge" class="knowledge-list"><p class="muted">请选择 Agent。</p></div><div class="form-grid"><label class="field"><span>文件名</span><input id="knowledgeFileName" placeholder="例如 product-notes.md"></label><label class="field wide"><span>内容</span><textarea id="knowledgeFileContent" class="textarea" placeholder="输入 Markdown / 文本知识内容"></textarea></label></div><div class="actions"><button type="button" onclick="saveKnowledgeFile()">保存知识文件</button><button class="secondary" type="button" onclick="reindexKnowledge()">重建索引</button><button class="secondary" type="button" onclick="refreshAgentKnowledge()">刷新知识库状态</button></div><div id="knowledgeMsg" class="notice hidden"></div></section></section></section><section class="tab-panel hidden" data-panel="rooms"><section class="card"><h2>托管房间</h2><div class="row"><button onclick="loadManagedRooms()">刷新房间</button></div><p class="muted">这里展示当前客户端接管的 Agent 正在服务的房间和最近对话，只读查看。</p><div id="roomsMsg" class="notice hidden"></div></section><section class="managed-layout"><div class="card"><h2>房间列表</h2><div id="managedRooms" class="room-list"></div></div><div class="card"><h2>对话内容</h2><div id="managedRoomDetail"></div></div></section></section>
    <section class="tab-panel hidden" data-panel="requests"><section class="card"><h2>正在处理的请求</h2><div id="runs"></div></section><section class="card"><h2>请求日志</h2><pre id="logs"></pre></section></section>
    <section class="tab-panel hidden" data-panel="health"><section class="card"><h2>环境检测</h2><div id="health"></div><button class="secondary" onclick="loadHealth()">重新检测</button></section></section>
  </section>
</main>
<script>
const tabDefs=[['account','账号配置'],['agents','Agent管理'],['create','新建Agent'],['rooms','托管房间'],['requests','请求'],['health','环境']]
let currentTab=localStorage.getItem('fcac_tab')||'account'
let serverAgents=[]
let managedRooms=[]
let selectedManagedRoomId=''
let managedRoomsLoaded=false
let serverAgentsLoaded=false
let serverAgentsLoading=false
let serverConnectAttempted=false
let editingAgentId=''
let latestLocalAgents=[]
async function api(path, init={}){const session=localStorage.getItem('fcac_session')||'';const r=await fetch('/api/local'+path,{...init,credentials:'include',headers:{'content-type':'application/json',...(session?{'x-agent-client-session':session}:{}),...(init.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw new Error(d.error?.message||d.message||'请求失败');return d.data??d}
async function boot(){renderTabs();try{await api('/session');showApp();refresh()}catch{document.getElementById('login').classList.remove('hidden');document.getElementById('app').classList.add('hidden')}}
function bindStaticActions(){}
function renderTabs(){const el=document.getElementById('tabs');el.innerHTML='';for(const [id,label] of tabDefs){const b=document.createElement('button');b.className='tab '+(id===currentTab?'active':'');b.textContent=label;b.onclick=()=>switchTab(id);el.appendChild(b)}switchTab(currentTab,false);bindStaticActions()}
function switchTab(id,save=true){currentTab=id;if(save)localStorage.setItem('fcac_tab',id);document.querySelectorAll('.tab-panel').forEach(p=>p.classList.toggle('hidden',p.dataset.panel!==id));document.querySelectorAll('.tab').forEach((b,i)=>b.classList.toggle('active',tabDefs[i][0]===id))}
function setLoginMsg(text,type='bad'){const el=document.getElementById('loginMsg');el.textContent=text;el.className='notice '+type}
async function login(){const btn=document.getElementById('loginBtn');try{btn.disabled=true;btn.textContent='登录中...';setLoginMsg('正在登录...','ok');const data=await api('/login',{method:'POST',body:JSON.stringify({password:document.getElementById('password').value})});if(data.sessionToken)localStorage.setItem('fcac_session',data.sessionToken);setLoginMsg('登录成功，正在加载...','ok');showApp();refresh()}catch(e){setLoginMsg(e.message||'登录失败','bad')}finally{btn.disabled=false;btn.textContent='登录'}}
async function logout(){await api('/logout',{method:'POST'}).catch(()=>{});localStorage.removeItem('fcac_session');location.reload()}
function showApp(){document.getElementById('login').classList.add('hidden');const app=document.getElementById('app');app.classList.remove('hidden');app.style.display='grid';app.style.gap='16px';renderTabs();bindStaticActions()}
async function refresh(){try{const data=await api('/state');render(data);loadHealth();ensureServerConnected(data.config)}catch(e){console.error(e);setLoginMsg(e.message||'加载失败','bad')}setTimeout(refresh,3000)}
function render(data){latestLocalAgents=data.config.agents||[];renderSummary(data);renderAccount(data.config);renderAgents(data.config.agents);renderServerAgents(data.config.agents);if(currentTab==='agentEdit')renderAgentEditPage();renderRuns(data.runtime);renderSettings(data.config);bindStaticActions()}
function renderAccount(cfg){const u=cfg.serverUser;document.getElementById('accountInfo').innerHTML=u?'<p class="account-name">已连接：<b>'+esc(u.nickname||u.username||u.id)+'</b></p><p class="muted">此账号发布的 Agent 会以该账号作为发布方/收费方。</p>':'<p class="account-name">未连接 FreeChat 账号</p><p class="muted">保存账号后，本客户端会自动连接并加载对应 Agent。</p>'}
function setInputValue(id,value){const el=document.getElementById(id);if(!el)return;if(document.activeElement===el)return;if(el.dataset.dirty==='1')return;el.value=value??''}
function renderSummary(data){setInputValue('serverUrl',data.config.serverUrl||'');setInputValue('serverUsername',data.config.serverUsername||'');document.getElementById('summary').innerHTML='<p>连接状态<br><span class="pill">'+(data.config.serverAuthToken?'已连接':'未连接')+'</span></p><p>服务地址<br><b>'+esc(data.config.serverUrl)+'</b></p><p>本客户端<br>'+esc(data.config.clientName)+' / '+esc(data.config.host)+':'+data.config.port+'</p><p>已接管 Agent<br><b>'+data.config.agents.length+'</b> 个</p>';document.getElementById('quickStatus').innerHTML='<p>接收状态<br><span class="pill">'+(data.runtime.workerRunning?'运行中':'已暂停')+'</span></p><p>正在处理<br><b>'+Object.keys(data.runtime.activeRuns).length+'</b> 个请求</p>'}
function renderAgents(list){const agentsEl=document.getElementById('agents');if(!agentsEl)return;agentsEl.innerHTML='';if(!list.length){agentsEl.innerHTML='<p class="muted">还没有由本客户端接管的 Agent。请在上方服务端 Agent 列表里选择“绑定到本客户端”。</p>';return}for(const a of list){const div=document.createElement('div');div.className='agent';div.innerHTML='<h3>'+esc(a.name||a.agentId)+'</h3><p class="muted">'+esc(a.agentId)+'</p><p>状态: <span class="pill">'+esc(a.status||'idle')+'</span> '+(a.enabled?'启用':'停用')+'</p><p>最近: '+(a.lastSeenAt?new Date(a.lastSeenAt).toLocaleString():'-')+'</p><p class="bad">'+esc(a.lastError||'')+'</p><div class="row"><button class="toggle"></button><button class="danger remove">取消接管</button></div>';const toggle=div.querySelector('.toggle');toggle.textContent=a.enabled?'停用':'启用';toggle.onclick=()=>toggleAgent(a.agentId,!a.enabled);div.querySelector('.remove').onclick=()=>removeAgent(a.agentId);agentsEl.appendChild(div)}}
function renderRuns(runtime){const runs=Object.values(runtime.activeRuns);document.getElementById('runs').innerHTML=runs.length?runs.map(r=>'<p><span class="pill">'+esc(r.type)+'</span> '+esc(r.agentId)+' / '+esc(r.runId)+'</p>').join(''):'<p class="muted">当前没有正在处理的请求。服务端 @Agent 或任务分派后会自动显示。</p>';document.getElementById('logs').textContent=runtime.logs.join(String.fromCharCode(10))}
function renderSettings(cfg){const el=document.getElementById('settings');if(!el)return;el.innerHTML=''}

function setServerMsg(text,type='ok'){const el=document.getElementById('serverMsg');el.textContent=text;el.className='notice '+type}
async function saveServerConfig(){await api('/config',{method:'PATCH',body:JSON.stringify({serverUrl:document.getElementById('serverUrl').value})});setServerMsg('服务器地址已保存','ok');refresh()}
function inputValue(id){const el=document.getElementById(id);return el?el.value:''}
async function saveAllConfig(connect=true){try{const body={serverUrl:inputValue('serverUrl'),serverUsername:inputValue('serverUsername'),serverPassword:inputValue('serverPassword')};await api('/config',{method:'PATCH',body:JSON.stringify(body)});if(document.getElementById('serverPassword'))document.getElementById('serverPassword').value='';document.querySelectorAll('input,select,textarea').forEach(el=>delete el.dataset.dirty);if(connect){setServerMsg('正在连接 FreeChat...','ok');await api('/server/auto-login',{method:'POST'});setServerMsg('已连接，正在加载 Agent','ok');serverAgentsLoaded=false;serverConnectAttempted=false;await loadServerAgents(false);managedRoomsLoaded=false;await loadManagedRooms();switchTab('agents')}else{setServerMsg('已保存','ok')}refresh()}catch(e){setServerMsg(e.message||'保存失败','bad')}}
async function saveAccountConfig(){try{const body={serverUrl:document.getElementById('serverUrl').value,serverUsername:document.getElementById('serverUsername').value,serverPassword:document.getElementById('serverPassword').value,adminPassword:document.getElementById('adminPasswordInput').value};await api('/config',{method:'PATCH',body:JSON.stringify(body)});document.getElementById('serverPassword').value='';document.getElementById('adminPasswordInput').value='';document.querySelectorAll('input,select,textarea').forEach(el=>delete el.dataset.dirty);setServerMsg('账号配置已保存，正在连接 FreeChat...','ok');await api('/server/auto-login',{method:'POST'});setServerMsg('已记录账号并连接成功；以后登录客户端会自动加载 Agent','ok');serverAgentsLoaded=false;serverConnectAttempted=false;await loadServerAgents(false);managedRoomsLoaded=false;await loadManagedRooms();switchTab('agents');refresh()}catch(e){setServerMsg(e.message||'保存账号配置失败','bad')}}
async function testServerConn(){try{const data=await api('/server/test',{method:'POST',body:JSON.stringify({serverUrl:document.getElementById('serverUrl').value})});setServerMsg('连接成功：'+JSON.stringify(data).slice(0,120),'ok')}catch(e){setServerMsg(e.message||'连接失败','bad')}}
async function loginFreeChatServer(){return saveAccountConfig()}
async function clearFreeChatAccount(){await api('/server/clear-account',{method:'POST'});serverAgents=[];serverAgentsLoaded=false;serverConnectAttempted=false;document.getElementById('serverUsername').value='';document.getElementById('serverPassword').value='';setServerMsg('已清除保存的 FreeChat 账号、密码和登录态','ok');refresh()}
async function logoutFreeChatServer(){return clearFreeChatAccount()}
function setAgentMsg(text,type='ok'){const el=document.getElementById('agentMsg');el.textContent=text;el.className='notice '+type}
async function ensureServerConnected(cfg){if(serverAgentsLoaded||serverAgentsLoading)return;if(cfg.serverAuthToken){loadServerAgents(true);return}if(cfg.serverUsername&&cfg.serverPassword&&!serverConnectAttempted){serverConnectAttempted=true;setServerMsg('正在自动连接 FreeChat 服务端...','ok');try{await api('/server/auto-login',{method:'POST'});setServerMsg('已自动连接 FreeChat 服务端','ok');await loadServerAgents(false);managedRoomsLoaded=false;await loadManagedRooms();switchTab('agents')}catch(e){setServerMsg(e.message||'自动连接服务端失败','bad')}}}
async function loadServerAgents(allowAutoLogin=true){if(serverAgentsLoading)return;serverAgentsLoading=true;try{const data=await api('/server/agents');serverAgents=data.agents||[];serverAgentsLoaded=true;renderServerAgents((await api('/state')).config.agents||[]);setAgentMsg('已自动加载当前账号对应的 Agent','ok')}catch(e){serverAgentsLoaded=false;if(allowAutoLogin){try{const state=await api('/state');if(state.config.serverUsername&&state.config.serverPassword){setAgentMsg('服务端登录已失效，正在自动重连...','ok');await api('/server/auto-login',{method:'POST'});return loadServerAgents(false)}}catch{}}setAgentMsg(e.message||'刷新 Agent 列表失败','bad')}finally{serverAgentsLoading=false}}
function renderServerAgents(localAgents){latestLocalAgents=localAgents||latestLocalAgents;const el=document.getElementById('serverAgents');if(!el)return;el.innerHTML='';if(!serverAgents.length){el.innerHTML='<p class="muted">当前账号还没有自己发布的 Agent。请先新建 Agent，或切换到对应发布方账号。</p>';return}for(const a of serverAgents){const bound=(localAgents||[]).some(x=>x.agentId===a.id);const isOwner=a.isOwner===true;const div=document.createElement('div');div.className='agent';div.innerHTML='<h3>'+esc(a.name)+'</h3><p>发布方：<b>'+esc(a.ownerName||a.ownerId||'-')+'</b> '+(isOwner?'<span class="pill">我的</span>':'<span class="pill">非本人</span>')+'</p><p class="muted">Agent ID：'+esc(a.id)+'</p><p><span class="pill">'+esc(a.deployment||'client')+'</span> '+(bound?'<span class="pill">已由本客户端接管</span>':'')+'</p><p>'+esc(a.description||'')+'</p><div class="row"><button class="bind"></button><button class="secondary list"></button><button class="secondary edit">进入编辑页</button></div>';const bindBtn=div.querySelector('.bind');bindBtn.textContent=bound?'已接管':(isOwner?'由本客户端接管执行':'只有发布方可接管');bindBtn.disabled=bound||!isOwner;bindBtn.title=!isOwner?'这个 Agent 的发布方不是当前账号，不能同步/接管到本客户端':'';bindBtn.onclick=()=>bindServerAgent(a.id);const listBtn=div.querySelector('.list');listBtn.textContent=a.marketListed?'取消上架':'上架到市场';listBtn.disabled=!isOwner;listBtn.title=!isOwner?'只有发布方可以上架':'';listBtn.onclick=()=>toggleMarketListAgent(a);const editBtn=div.querySelector('.edit');editBtn.disabled=a.canEdit===false;editBtn.title=a.canEdit===false?'只有发布方/有权限用户可编辑':'';editBtn.onclick=()=>openAgentEditor(a);el.appendChild(div)}}

async function toggleMarketListAgent(agent){try{setAgentMsg((agent.marketListed?'正在取消上架':'正在上架到市场')+'...','ok');await api('/server/agents/'+encodeURIComponent(agent.id),{method:'PATCH',body:JSON.stringify({marketListed:!agent.marketListed})});setAgentMsg(agent.marketListed?'已取消上架':'已上架到市场','ok');await loadServerAgents(false)}catch(e){setAgentMsg(e.message||'上架状态更新失败','bad')}}

function setAgentEditMsg(text,type='ok'){const el=document.getElementById('agentEditMsg');if(!el)return;el.textContent=text;el.className='notice '+type}
function openAgentEditor(agent){editingAgentId=typeof agent==='string'?agent:(agent?.id||'');switchTab('agentEdit');renderAgentEditPage();refreshAgentKnowledge()}
function renderAgentEditPage(){if(currentTab!=='agentEdit')return;const a=serverAgents.find(x=>x.id===editingAgentId);const title=document.getElementById('agentEditTitle');if(!title)return;if(!a){title.textContent='编辑 Agent';setAgentEditMsg('请选择要编辑的 Agent，或先刷新 Agent 列表','bad');return}title.textContent='编辑 Agent：'+(a.name||a.id);setInputValue('editAgentName',a.name||'');setInputValue('editAgentDesc',a.description||'');setInputValue('editAgentSpecialties',(a.specialties||[]).join? a.specialties.join(',') : (a.specialties||''));const dep=document.getElementById('editAgentDeployment');if(dep)dep.value='client';const local=latestLocalAgents.some(x=>x.agentId===a.id);const bindBtn=document.getElementById('bindAgentFromEditBtn');if(bindBtn){bindBtn.disabled=local||a.isOwner!==true;bindBtn.textContent=local?'已由本客户端接管':'由本客户端接管执行';bindBtn.onclick=()=>bindServerAgent(a.id)}const saveBtn=document.getElementById('saveAgentBtn');if(saveBtn)saveBtn.onclick=()=>saveAgent(a.id)}
async function saveAgent(id){try{const body={name:document.getElementById('editAgentName').value,deployment:'client',description:document.getElementById('editAgentDesc').value,specialties:document.getElementById('editAgentSpecialties').value.split(',').map(s=>s.trim()).filter(Boolean),config:{tools:{chat:true,tasks:true,files:true}}};const result=await api('/server/agents/'+encodeURIComponent(id),{method:'PATCH',body:JSON.stringify(body)});setAgentEditMsg('Agent 已保存，发布方/收费方仍为当前 FreeChat 账号','ok');await loadServerAgents(false);editingAgentId=result.agent?.id||id;renderAgentEditPage();refreshAgentKnowledge()}catch(e){setAgentEditMsg(e.message||'保存 Agent 失败','bad')}}
async function refreshAgentKnowledge(){if(!editingAgentId)return;const el=document.getElementById('agentKnowledge');if(!el)return;el.innerHTML='<p class="muted">正在读取客户端知识库状态...</p>';try{const data=await api('/agents/'+encodeURIComponent(editingAgentId)+'/knowledge');renderAgentKnowledge(data)}catch(e){el.innerHTML='<p class="bad">'+esc(e.message||'读取知识库失败')+'</p>'}}
function setKnowledgeMsg(text,type='ok'){const el=document.getElementById('knowledgeMsg');if(!el)return;el.textContent=text;el.className='notice '+type}
function renderAgentKnowledge(data){const el=document.getElementById('agentKnowledge');if(!el)return;const local=data.localAgent;const k=data.knowledge||{};let html='<div class="knowledge-item"><b>归属边界</b><p class="muted">Agent 知识库存在 Agent Client 本地；FreeChat Server 只保存 Agent 身份、发布、调度和运行记录，不保存知识库正文。</p></div>';if(!local)html+='<div class="knowledge-item"><b>未接管</b><p class="muted">这个 Agent 当前未由本客户端接管。保存知识文件会创建预留目录；接管后运行时会自动读取该目录说明。</p></div>';if(k.dir){html+='<div class="knowledge-item"><b>知识库目录</b><p class="muted">'+esc(k.dir)+'</p><p><span class="pill">'+(k.fileCount||0)+' 个文件</span> <span class="pill">'+formatBytes(k.totalSize||0)+'</span></p></div>';html+='<div class="knowledge-item"><b>文件列表</b>'+(k.files?.length?'<div class="knowledge-list">'+k.files.map(i=>'<p class="muted">'+(i.type==='dir'?'📁 ':'📄 ')+esc(i.path)+' '+(i.type==='file'?' · '+formatBytes(i.size||0)+' <button class="danger" style="min-height:28px;padding:4px 8px;margin-left:8px" onclick="deleteKnowledgeFile(\''+encodeURIComponent(i.path)+'\')">删除</button>':'')+'</p>').join('')+'</div>':'<p class="muted">暂无知识文件。</p>')+'</div>'}for(const s of data.stores||[]){html+='<div class="knowledge-item"><b>'+esc(s.label)+'</b><p class="muted">'+esc(s.path)+'</p><p><span class="pill">'+(s.exists?'已存在':'未创建')+'</span> <span class="pill">'+esc(s.kind)+'</span></p><p class="muted">文件 '+(s.fileCount||0)+' 个 / 目录 '+(s.dirCount||0)+' 个'+(s.updatedAt?' / 最近更新 '+new Date(s.updatedAt).toLocaleString():'')+'</p></div>'}el.innerHTML=html}
function formatBytes(n){n=Number(n||0);if(n<1024)return n+' B';if(n<1024*1024)return (n/1024).toFixed(1)+' KB';return (n/1024/1024).toFixed(1)+' MB'}
async function saveKnowledgeFile(){if(!editingAgentId){setKnowledgeMsg('请先选择 Agent','bad');return}try{const name=document.getElementById('knowledgeFileName').value.trim();const content=document.getElementById('knowledgeFileContent').value;if(!name){setKnowledgeMsg('请填写文件名','bad');return}await api('/agents/'+encodeURIComponent(editingAgentId)+'/knowledge',{method:'POST',body:JSON.stringify({name,content})});document.getElementById('knowledgeFileName').value='';document.getElementById('knowledgeFileContent').value='';setKnowledgeMsg('知识文件已保存','ok');refreshAgentKnowledge()}catch(e){setKnowledgeMsg(e.message||'保存知识文件失败','bad')}}
async function deleteKnowledgeFile(encodedPath){if(!editingAgentId)return;if(!confirm('删除这个知识文件？'))return;try{await api('/agents/'+encodeURIComponent(editingAgentId)+'/knowledge/files/'+encodedPath,{method:'DELETE'});setKnowledgeMsg('知识文件已删除','ok');refreshAgentKnowledge()}catch(e){setKnowledgeMsg(e.message||'删除知识文件失败','bad')}}
async function reindexKnowledge(){if(!editingAgentId){setKnowledgeMsg('请先选择 Agent','bad');return}try{await api('/agents/'+encodeURIComponent(editingAgentId)+'/knowledge/reindex',{method:'POST'});setKnowledgeMsg('索引已重建','ok');refreshAgentKnowledge()}catch(e){setKnowledgeMsg(e.message||'重建索引失败','bad')}}
async function bindServerAgent(id){try{await api('/server/agents/'+encodeURIComponent(id)+'/bind',{method:'POST'});setAgentMsg('已由本客户端接管执行','ok');managedRoomsLoaded=false;await loadServerAgents();await loadManagedRooms();refresh()}catch(e){setAgentMsg(e.message||'绑定失败','bad')}}




function setCreateAgentMsg(text,type='ok'){const el=document.getElementById('createAgentMsg');if(!el)return;el.textContent=text;el.className='notice '+type}
async function createAgentFromStandaloneForm(){try{const name=document.getElementById('createAgentName').value.trim();if(!name){setCreateAgentMsg('请先填写 Agent 名称','bad');return}const body={name,roleType:'specialist',deployment:'client',description:document.getElementById('createAgentDesc').value,specialties:document.getElementById('createAgentSpecialties').value.split(',').map(s=>s.trim()).filter(Boolean),config:{tools:{chat:true,tasks:true,files:true}}};setCreateAgentMsg('正在创建 Agent...','ok');const result=await api('/server/agents',{method:'POST',body:JSON.stringify(body)});const createdId=result.agent?.id||result.id;if(createdId&&body.deployment==='client'){setCreateAgentMsg('Agent 已创建，正在接管到本客户端...','ok');await bindServerAgent(createdId)}else{setCreateAgentMsg('Agent 已创建','ok')}document.getElementById('createAgentName').value='';document.getElementById('createAgentDesc').value='';document.getElementById('createAgentSpecialties').value='';await loadServerAgents(false);managedRoomsLoaded=false;await loadManagedRooms();switchTab('agents')}catch(e){setCreateAgentMsg(e.message||'创建 Agent 失败','bad')}}

function setRoomsMsg(text,type='ok'){const el=document.getElementById('roomsMsg');if(!el)return;el.textContent=text;el.className='notice '+type}
async function loadManagedRooms(){try{const data=await api('/server/managed-rooms');managedRooms=data.rooms||[];managedRoomsLoaded=true;if(!selectedManagedRoomId||!managedRooms.some(r=>r.id===selectedManagedRoomId))selectedManagedRoomId=managedRooms[0]?.id||'';renderManagedRooms();setRoomsMsg('托管房间已刷新','ok')}catch(e){managedRoomsLoaded=false;setRoomsMsg(e.message||'加载托管房间失败','bad')}}
function renderManagedRooms(){const list=document.getElementById('managedRooms');const detail=document.getElementById('managedRoomDetail');if(!list||!detail)return;list.innerHTML='';if(!managedRooms.length){list.innerHTML='<p class="muted">当前还没有房间使用本客户端接管的 Agent。</p>';detail.innerHTML='<p class="muted">选择左侧房间后查看最近对话。</p>';return}for(const room of managedRooms){const btn=document.createElement('button');btn.className='room-item '+(room.id===selectedManagedRoomId?'active':'');btn.innerHTML='<b>'+esc(room.name)+'</b><p class="muted">'+esc((room.agents||[]).map(a=>a.name).join('、')||'托管 Agent')+'</p><p><span class="pill">'+(room.messages?.length||0)+' 条消息</span></p>';btn.onclick=()=>{selectedManagedRoomId=room.id;renderManagedRooms()};list.appendChild(btn)}const room=managedRooms.find(r=>r.id===selectedManagedRoomId)||managedRooms[0];detail.innerHTML='<div><h3>'+esc(room.name)+'</h3><p class="muted">托管 Agent：'+esc((room.agents||[]).map(a=>a.name+'('+(a.connectorStatus||'-')+')').join('、')||'-')+'</p></div><div class="message-list">'+((room.messages||[]).length?room.messages.map(renderManagedMessage).join(''):'<p class="muted">这个房间还没有消息。</p>')+'</div>'}
function renderManagedMessage(m){const role=m.actorRole==='ai'?'ai':(m.kind&&m.kind!=='text'?'system':'');const text=formatManagedContent(m);if(!text)return '';return '<div class="msg '+role+'"><div class="msg-head"><span>'+esc(m.actorName||m.actorId||'-')+'</span><span>'+new Date(m.createdAt).toLocaleString()+'</span></div><div class="msg-content">'+esc(text)+'</div></div>'}
function formatManagedContent(m){const raw=String(m.content||'').trim();if(raw){const parsed=parseMaybeJson(raw);if(parsed){if(parsed.type==='system'&&parsed.subtype==='init')return '';if(parsed.type==='system')return parsed.message?('系统：'+parsed.message):'';if(parsed.text||parsed.message)return String(parsed.text||parsed.message)}if(raw.includes('"type":"system"')&&raw.includes('"subtype":"init"'))return '';return raw}if(m.payload?.agentStream){const items=m.payload.agentStream.activities||[];const last=items[items.length-1];return last?.text?'状态：'+last.text:'状态：Agent 正在处理'}return ''}
function parseMaybeJson(text){if(!text||!['{','['].includes(text[0]))return null;try{return JSON.parse(text)}catch{return null}}
function formatPayload(payload){if(!payload)return '';if(payload.agentStream)return formatManagedContent({payload});try{return JSON.stringify(payload)}catch{return String(payload||'')}}

function setSettingsMsg(text,type='ok'){const el=document.getElementById('settingsMsg');if(!el)return;el.textContent=text;el.className='notice '+type}
async function saveClientSettings(){try{await api('/config',{method:'PATCH',body:JSON.stringify({clientName:document.getElementById('clientNameInput').value,maxConcurrency:document.getElementById('maxConcurrencyInput').value,pollIntervalMs:document.getElementById('pollIntervalInput').value})});setSettingsMsg('设置已保存','ok');refresh()}catch(e){setSettingsMsg(e.message||'保存失败','bad')}}

async function loadHealth(){const h=await api('/health');document.getElementById('health').innerHTML='<p>Node: '+esc(h.node.text)+'</p><p>Claude: <span class="'+(h.claude.ok?'ok':'bad')+'">'+esc(h.claude.text||'未安装')+'</span></p><p>cc-switch: <span class="'+(h.ccSwitch.ok?'ok':'bad')+'">'+esc(h.ccSwitch.text||'未安装')+'</span></p><p class="muted">检测时间：'+new Date(h.checkedAt).toLocaleString()+'</p>'}
async function toggleAgent(agentId,enabled){await api('/agents/'+encodeURIComponent(agentId),{method:'PATCH',body:JSON.stringify({enabled})});refresh()}
async function removeAgent(agentId){if(confirm('取消本客户端接管这个 Agent？')){await api('/agents/'+encodeURIComponent(agentId),{method:'DELETE'});refresh()}}
async function resumeReceiving(){await api('/worker/start',{method:'POST'});refresh()} async function pauseReceiving(){await api('/worker/stop',{method:'POST'});refresh()}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
document.addEventListener('input',e=>{if(e.target&&['INPUT','SELECT','TEXTAREA'].includes(e.target.tagName))e.target.dataset.dirty='1'})
document.getElementById('password').addEventListener('keydown',e=>{if(e.key==='Enter')login()})
boot()
</script>
</body></html>`
}
